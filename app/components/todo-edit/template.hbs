<div class="form-group">
  {{input type="text" value=item.title class="form-control"}}
</div>
<div class="form-group">
  <button class="btn btn-info"{{action 'saveItem' item}}>
    {{fa-icon "cloud-upload"}} Save
  </button>
  {{fa-icon "times" class="pull-right"
    click=(action 'closePanel' item)
  }}
</div>